{% extends 'base.html.twig' %}

{% block title %}Personne{% endblock %}

{% block body %}
    <h1 class="pb-2 border-bottom mb-4">Personne</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personne.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ personne.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_personne_index') }}">Retour</a>

    <a href="{{ path('app_personne_edit', {'id': personne.id}) }}">Modifier</a>

    {{ include('personne/_delete_form.html.twig') }}
{% endblock %}
