{{ form_start(form) }}

    <div class="form-group">
    {{ form_label(form.nom, 'Groupe ',{'label_attr': {'class': 'col-sm-12 col-form-label'}}) }}
    <div class="input-group_ col-md-5">
        
        {{ form_widget(form.nom, {'attr': {'class': 'form-control  ','placeholder' : 'Groupe '}}) }}
    </div>
    </div>
    <div class="form-group">
        {{ form_label(form.createAt, 'Date d\'ajout', {'label_attr': {'class': 'col-sm-10 col-form-label'}}) }}
        <div class="col-sm-12">
            {{ form_widget(form.createAt, {'attr': {'class': 'form-controls'}}) }}
        </div>
    </div>
    <button class="btn btn-primary my-4">{{ button_label|default('Enrégister') }}</button>
{{ form_end(form) }}
