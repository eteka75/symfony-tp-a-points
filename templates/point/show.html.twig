{% extends 'base.html.twig' %}

{% block title %}Point{% endblock %}

{% block body %}
    <h1 class="pb-2 border-bottom mb-4">Point</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ point.id }}</td>
            </tr>
            <tr>
                <th>Point</th>
                <td>{{ point.point }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ point.createdAt ? point.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_point_index') }}">back to list</a>

    <a href="{{ path('app_point_edit', {'id': point.id}) }}">edit</a>

    {{ include('point/_delete_form.html.twig') }}
{% endblock %}
